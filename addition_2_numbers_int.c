//Addition of 2 numbers using int 

#include<stdio.h>

int main()
{
    int a, b, sum;
    printf("Enter The values of a and b:\n");
    scanf("%d%d", &a, &b);
    sum = a + b;//addition expression
    printf("Sum=%d", sum);
    return 0;
}